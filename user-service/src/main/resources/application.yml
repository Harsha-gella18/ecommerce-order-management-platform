server:
  port: ${SERVER_PORT:8082}

spring:
  application:
    name: user-service
  data:
    mongodb:
      uri: ${USER_MONGODB_URI:${SPRING_DATA_MONGODB_URI:mongodb://127.0.0.1:27017/user_db}}

jwt:
  secret: ${JWT_SECRET:ecommerce-jwt-secret-key-for-development-min-256-bits-long-for-hs256-algorithm-safety}
